//Generated by WriteToUsemodelpageparams_tsx - useModelPageParams.tsx
import { useURL } from "@/hooks/useURL";
import { TaskTemplateSearchParams } from "@/interfaces/TaskTemplateInterfaces";
import { DEFAULT_LIMIT } from "@/utils/constants";
import { DEFAULT_SORT_BY } from "@/utils/constants/TaskTemplateConstants";

export const useTaskTemplatePageParams = () => {
  const { query, pathname, router } = useURL<TaskTemplateSearchParams>();

  //Generated by GetAllSearchParamsBySeqModel
const q = query["q"] || "";const taskCategory = query["taskCategory"] || "";;const taskInterval = query["taskInterval"] || "";
  const sort = query["sort"] || DEFAULT_SORT_BY;
  const limit = query["limit"] || DEFAULT_LIMIT;

  return {
    query,
    pathname,
    router,
    params: { 
     //Generated by GetAllFilterQueryNameBySeqModel
q,
taskCategory,
taskInterval,
     sort, 
     limit 
    },
  };
};
