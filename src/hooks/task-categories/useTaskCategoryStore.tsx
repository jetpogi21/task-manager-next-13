//Generated by WriteToUsemodelstore_ts - useModelStore.ts
import { Slice, modelSlice } from "@/lib/zustand-slice";
import { TaskCategoryConfig } from "@/utils/config/TaskCategoryConfig";
import { create } from "zustand";

const useTaskCategoryStore = create<Slice<Record<string, unknown>>>((set) =>
  modelSlice(set, TaskCategoryConfig.sortString)
);

export { useTaskCategoryStore };
