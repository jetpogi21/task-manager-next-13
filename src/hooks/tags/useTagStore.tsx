//Generated by WriteToUsemodelstore_ts - useModelStore.ts
import { Slice, modelSlice } from "@/lib/zustand-slice";
import { TagConfig } from "@/utils/config/TagConfig";
import { create } from "zustand";

const useTagStore = create<Slice<Record<string, unknown>>>((set) =>
  modelSlice(set, TagConfig.sortString)
);

export { useTagStore };
