//Generated by WriteToModelsinglecolumn_tsx - ModelSingleColumn.tsx
import { TaskTemplateModel } from "@/interfaces/TaskTemplateInterfaces";
import { CellContext } from "@tanstack/react-table";

interface TaskTemplateSingleColumnProps<T> {
  cell: CellContext<T, unknown>;
}

const TaskTemplateSingleColumn = <T,>({
  cell,
}: TaskTemplateSingleColumnProps<T>) => {
  const taskTemplate = cell.row.original as TaskTemplateModel;
  return <div className="flex gap-2">Edit Me Bruh!</div>;
};

export default TaskTemplateSingleColumn;
