//Generated by WriteToModellibs_ts - ModelLibs.ts
import { TagFormikShape } from "@/interfaces/TagInterfaces";
import { Tag } from "@/models/TagModel";
import { PRIMARY_KEY } from "@/utils/constants/TaskIntervalConstants";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

interface ITag extends Omit<TagFormikShape, "touched" | "index"> {}

//Reusable functions
export const createTag = async (
  tag: Omit<ITag, typeof PRIMARY_KEY>,
  t: Transaction
) => {
  return await Tag.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      name: tag.name!,
    },
    { transaction: t }
  );
};

export const updateTag = async (
  tag: ITag,
  primaryKey: keyof ITag,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await Tag.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      name: tag.name!,
    },
    {
      where: { [primaryKey]: primaryKeyValue || tag[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deleteTags = async (
  primaryKey: keyof Omit<TagFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await Tag.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
